services:
  - type: web
    name: hamar-bazar-map
    runtime: python
    buildCommand: pip install -r requirements.txt && python generate_markers.py
    startCommand: gunicorn app:app
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
